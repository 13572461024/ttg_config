server:
  port: 8010
max-http-header-size: 1048576
  tomcat: 
    max-http-post-size: 1048576 
spring:
  application:
    name: TTG-COMMON #name不能使用下划线
eureka:
  client:
    service-url:
     defaultZone: http://localhost:7001/eureka #告诉服务提供者要把服务注册到哪儿
  instance:
        prefer-ip-address: true #显示客户端真实ip